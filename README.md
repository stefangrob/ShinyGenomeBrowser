# ShinyGenomeBrowser
A shiny-based genome browser

Dependencies:
You need to install samtools (http://www.htslib.org/download/) and awk (preinstalled on most Unix systems)
R-packages required are indicated in the first lines of the app.R file

Run:
Run code in app.R in a R instance or run the app.R file in RStudio
